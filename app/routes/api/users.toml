# include controller and action
is_resource = true # whether this route is a resource route or not

[request.GET]
controller = "api/user"
action = "list"

[request.POST]
controller = "api/user"
action = "update"
